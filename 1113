RequestDispatcher
  クライアントからの要請をサーブレット内で、、、
  
「String(文字列)の空白消去」

１．.trim()メソッド

	ー>文字列の前後の空白消去
	文字の間は空白が残る

２．.replaceAll()メソッド

	ー>.replaceAll("正規表現","代替の文字列"); 
	前の正規表現の該当する文字列があると
	後の部分に変換するメソッド
	空白を消去したいなら、後ろの部分を""にする。

	cg. \\p{Z｝　の意味　
	後で調べること

「HttpServletResponse, HttpServletRequest」

	１．.set(get)Attribute()
	クラス(bean又はDTO)で入れたり抜いたりすることができる(単純にStringタイプを運ぶ｢.set(get)Parameter()｣より活用性が高い)


「正規表現」

	+と＊の違い
	+は少なくとも一桁以上の値がある場合のみを検査ー>値がないと検査自体ができないのでfalseを戻す
	*は値がなくても(空白)一応検査するー>true値をリターン







