무엇이 궁금한가.
1. 필드와 추출필드의 개념이해가 올바른가?
- 필드는 입/출력에 따라서 그 내용이 다르다.
- 입력의 경우, 읽어들인 데이터(txt나 sql로 조회한 데이터)를 
  데이터패턴 객체의 해당 인덱스의 데이터에 어떻게 넣을 것인가.
  즉, 어떤 컬럼들을 넣을 것인가를 지정하는 것, 그 컬럼들을 명시하는 란
- 출력의 경우, 데이터패턴 객체의 해당 인덱스의 데이터로부터 어떤 컬럼들을 사용하여
  출력할 것인가를 지정

- 추출필드란

필드와 추출필드의 설명에 따라
필드는 - 열(컬럼) / 추출필드는 - 행 이라고 생각했는데
뭔가 다른가?


----------------------------------------
필드 추출필드 가로 세로 개념으로 이해해야하나???

검증을 위한 설정
필드와 추출필드를 다르게 설정하고 입출력했을때의 결과가 어케나오는가.


----------------------------------------


키워드를 격납(객체에 값 넣음)하는 이유
-> sql 쿼리 날릴 때 변수로 사용하려구.

분기처리시, 왜 예제의 UPDATE문 부분에선
필드와 추출필드를 공란으로 두었는가.
-> 업데이트하기 위해서 데이터패턴 객체에 넣어둔 데이터(in_jissyu_3.txt파일의 데이터)가
필요하지 않는가? 지정을 안해두면 어케 알아서 찾지?
데이터패턴이 아닌, 키워드의 값들을 사용하기 때문에 필드설정 필요없음.
상기 인덱스에서 이미 각 키워드에 값들을 넣어놨고
그것은 sql에 직접 씀으로써 불러오기 때문에. 
